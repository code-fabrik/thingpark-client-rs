# ThingPark Wireless Portal Client

## State

This is a work in progress. Do not use for anything important.

## Development

* Install openssl and pkg-config: `sudo apt-get install pkg-config libssl-dev`
* Install dependencies: `cargo build`
